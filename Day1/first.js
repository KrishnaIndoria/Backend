const {sum,sub} = require("./second");
sum(3,4);
sub(9,6)
console.log("Hello i am first");


// this is old approach (using require),but still used even today in industry
// this is CommonJS (CJS) method , and by default NODEjs supports this old approach