const {sum,sub,mul} = require("./Calculator");

sum(3,4);
sub(5,3);
mul(5,7);


// here instead of writing rrequire again n again , we write tht in index.js 
// n import index.js here
// by default index.js is imported from calculator